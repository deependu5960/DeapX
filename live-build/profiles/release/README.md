# Release Profile

## Purpose
Production-ready build for end-user deployment.

## Characteristics
- Optimized package selection
- Clean boot experience
- Minimal debugging output
- Production-grade configuration

## Usage
Set `PROFILE=release` in `config/deapx.conf`